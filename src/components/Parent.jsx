import React from "react";

const Parent = () => {
  return <div></div>;
};

export default Parent;

int index = -1;
inti = 0; 
while(size > 0) 
{ if(a[i] == key) { 
  index = i; break; 
} if(a[i] > key)) {
   break; 
  } i++; 
} 
return index;
